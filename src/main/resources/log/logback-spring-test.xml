<?xml version="1.0" encoding="UTF-8"?>
<!-- Logback configuration. See http://logback.qos.ch/manual/index.html -->
<configuration scan="true" scanPeriod="10 seconds">
    <jmxConfigurator/>
    <!--<include resource="org/springframework/boot/logging/logback/base.xml"/>-->
    <!-- 日志目录 -->
    <property name="LOG_HOME" value="/apps/testapp/logs/ib-web"/>

    <!-- 彩色日志 -->
    <!-- 彩色日志依赖的渲染类 -->
    <conversionRule conversionWord="clr"
                    converterClass="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="wex"
                    converterClass="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>
    <conversionRule conversionWord="wEx"
                    converterClass="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter"/>
    <!-- 彩色日志格式 -->
    <property name="CONSOLE_LOG_PATTERN"
              value="${CONSOLE_LOG_PATTERN:-%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(-){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(%6L){magenta} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}}"/>

    <!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符-->
    <property name="File_LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} %5p - [%15.15t] %-40.40logger{39} %6L : %m%n"/>
    <!-- 日志文件最大的大小 -->
    <property name="File_LOG_MAX_SIZE" value="1GB"/>
    <!-- 日志最大保存天数 -->
    <property name="File_LOG_MAX_HISTORY" value="30"/>
    <!-- 日志文件名称 -->
    <property name="ALL_File_NAME" value="%d{yyyy-MM-dd}/all-%d{HH}.log"/>
    <property name="ERR_File_NAME" value="%d{yyyy-MM-dd}/error.log"/>

    <!-- 控制台输出, 生产环境去除 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- 按照每小时生成日志文件,打印全部日志 -->
    <appender name="ALL_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!--日志文件输出的文件名-->
            <FileNamePattern>${LOG_HOME}/${ALL_File_NAME}</FileNamePattern>
            <!--日志文件保留天数-->
            <MaxHistory>${File_LOG_MAX_HISTORY}</MaxHistory>
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${File_LOG_PATTERN}</pattern>
        </encoder>
        <!--日志文件最大的大小-->
        <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <MaxFileSize>${File_LOG_MAX_SIZE}</MaxFileSize>
        </triggeringPolicy>
    </appender>

    <!-- 按照每小时生成日志文件,只打印error日志 -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- 临界值过滤器，过滤掉低于指定临界值的日志。当日志级别等于或高于临界值时，过滤器返回NEUTRAL；当日志级别低于临界值时，日志会被拒绝。 -->
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!--日志文件输出的文件名-->
            <FileNamePattern>${LOG_HOME}/${ERR_File_NAME}</FileNamePattern>
            <!--日志文件保留天数-->
            <MaxHistory>${File_LOG_MAX_HISTORY}</MaxHistory>
        </rollingPolicy>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${File_LOG_PATTERN}</pattern>
        </encoder>
        <!--日志文件最大的大小-->
        <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <MaxFileSize>${File_LOG_MAX_SIZE}</MaxFileSize>
        </triggeringPolicy>
    </appender>

    <logger name="com.ideal" level="debug" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <!--<appender-ref ref="ALL_FILE"/>-->
        <!--<appender-ref ref="ERROR_FILE"/>-->
    </logger>
    <root level="info">
        <!-- 控制台打印info及以上级别系统日志 -->
        <appender-ref ref="CONSOLE"/>
        <!-- 文件记录error级别系统日志 -->
        <!--<appender-ref ref="ERROR_FILE"/>-->
    </root>
</configuration>
